---
pagination:
    data: articles
    size: 5
---

{% extends "_includes/default.njk" %}
{% block title %}Jocarifeta{% endblock %}
{% block content %}
  <div class="max-w-screen-sm mx-auto pb-12">
    {% for article in pagination.items %}
      <h1>{{ article.title }}</h1>
      <p>By
        <a>{{ article.author }}</a>
        –
        <span>{{ article.created | dayjs }}</span>
      </p>
      <p class="py-4 whitespace-pre-line">{{ article.content }}</p>
      <div class="text-right">
        <a href="blog/{{ article.slug }}">Read article</a>
      </div>
    {% endfor %}
  </div>
  <div class="absolute bottom-0 w-full bg-yellow h-12 container flex items-center justify-center">
    <p>you’ve reached the end of my journey…</p>
  </div>
{% endblock %}
